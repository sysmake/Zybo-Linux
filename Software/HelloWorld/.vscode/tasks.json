{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "type": "shell",
            "linux": {
                "command": "${Zynq_gcc} ${file} -o ${fileBasenameNoExtension}"
            },
        }
    ]
}