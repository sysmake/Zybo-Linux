{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compile",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "preLaunchTask": "Compile"
        }
    ]
}